# TextEditor 用户手册

Windows 终端文本编辑器，C99 编写，纯内核 API，无外部依赖。

---

## 目录

1. [编译与运行](#编译与运行)
2. [基本操作](#基本操作)
3. [文件操作](#文件操作)
4. [光标移动](#光标移动)
5. [编辑操作](#编辑操作)
6. [搜索](#搜索)
7. [语法高亮](#语法高亮)
8. [界面布局](#界面布局)
9. [已知限制](#已知限制)

---

## 编译与运行

### 编译（项目根目录）

```
mkdir build && cd build
cmake .. -G "Ninja"
ninja
```

### 运行

```
TextEditor.exe              // 新建未命名文件
TextEditor.exe main.c       // 打开已有文件
```

### 环境要求

- Windows 10+（需要 VT 终端支持）
- CMD、PowerShell、Windows Terminal 均可

---

## 基本操作

编辑器启动后自动进入**备用屏幕缓冲区**，退出时自动恢复原始终端内容。

底部有**状态栏**（文件名、行数、修改标记）和**消息栏**（操作提示，5 秒自动消失）。

---

## 文件操作

| 按键 | 功能 |
|------|------|
| `Ctrl+S` | 保存文件 |
| `Ctrl+Q` | 退出编辑器 |

- 保存时使用当前文件名（如未指定则默认为 `untitled.txt`）
- 保存成功/失败会在消息栏提示

---

## 光标移动

| 按键 | 功能 |
|------|------|
| `↑` `↓` `←` `→` | 方向键移动光标 |
| `Home` | 跳到行首 |
| `End` | 跳到行尾 |
| `PgUp` / `PgDn` | 上下翻页 |

光标不会超出文件内容的边界（空文件时光标在起始位置）。

---

## 编辑操作

| 按键 | 功能 |
|------|------|
| `Enter` | 换行 |
| `Backspace` | 删除光标前一个字符 |
| `Delete` | 删除光标处字符 |
| `Tab` | 插入制表符（显示为空格，缩进宽度由语言决定） |
| 可打印字符 | 直接输入 |

- **行首 Enter**：插入空行，光标留在原内容行
- **行尾 Backspace**：当前行合并到上一行末尾
- **行尾 Delete**：下一行合并到当前行末尾

---

## 搜索

| 按键 | 功能 |
|------|------|
| `Ctrl+F` | 打开搜索框，输入关键词后回车 |
| `Ctrl+G` | 查找下一个匹配 |

### 搜索流程

1. 按 `Ctrl+F`，底部消息栏出现 `Search: %s (ESC to cancel)`
2. 输入搜索词，回车确认
3. 所有匹配行均以**蓝色高亮**标记，光标跳转到第一个匹配位置
4. 按 `Ctrl+G` 跳到下一个匹配
   - 同一行有多个匹配时，依次跳转
   - 当前行搜完自动跳到下一行
   - 绕回到文件开头时提示 "Search wrapped around"
   - 全文无更多匹配时提示 "No more matches"
5. 按 `ESC` 取消搜索，蓝色高亮清除

---

## 语法高亮

根据文件后缀名自动识别语言，支持的语法：

| 语言 | 后缀 |
|------|------|
| C / C++ | `.c` `.h` `.cpp` `.hpp` `.cc` |
| Python | `.py` |
| Rust | `.rs` |
| Go | `.go` |
| JavaScript / TypeScript | `.js` `.ts` |

### 高亮颜色

| 颜色 | 用途 |
|------|------|
| 青色 (36) | 注释（单行 + 多行） |
| 黄色 (33) | 类型关键字（`int`、`void`、`float` 等） |
| 绿色 (32) | 控制流关键字（`if`、`while`、`return` 等） |
| 蓝色 (34) | 搜索匹配 |
| 白色 (37) | 普通文本（默认） |

多行注释（`/* */`）跨行时自动延续上一行的注释状态，关闭后后续行自动恢复正常高亮。

### 缩进宽度

每种语言可独立设置 `tab_stop` 缩进宽度：
- C/C++、Python、Rust、JS/TS：4 空格
- Go：8 空格

---

## 界面布局

```
┌──────────────────────────────────────────┐
│ 行号 编辑区域                                │  ← 内容区（screen_rows - 2 行）
│  1  #include <stdio.h>                    │
│  2  int main() {                          │
│  3      int a = 10;                       │
│  4  }                                     │
│                                          │
│ "test.c"  4 行  [修改]                     │  ← 状态栏（文件名 / 行数 / 修改标记）
│ Search: foo (ESC to cancel)              │  ← 消息栏（提示 / 搜索结果）
└──────────────────────────────────────────┘
```

- **行号栏**：左侧显示行号，青色
- **波浪线 `~`**：文件行数之外的屏幕行
- **状态栏**：反色显示，内容为 `"文件名"  行数  [修改]`
- **消息栏**：正常显示，用于搜索输入、操作反馈等

---

## 已知限制

- 不支持 Ctrl+C 复制 / Ctrl+V 粘贴（Ctrl+C 作为保留未绑定；粘贴走 Windows 终端右键菜单即可）
- 鼠标支持未实现（不能点击定位光标）
- 字符串字面量高亮（`"..."` `'...'`）未实现
- 不支持 UTF-8 中文输入（显示可能乱码）
- 搜索框不支持历史记录
- 不支持行跳转（无 `Ctrl+G` 行号跳转，注意 `Ctrl+G` 已用于"查找下一个"）
- `seq[8]` 缓冲区最大支持 6 字节转义序列，极少数终端序列可能被截断

---

*TextEditor — 基于 kilo 架构，移植到 Windows 控制台 API*
